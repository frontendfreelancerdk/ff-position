import { AfterViewInit, ElementRef, EventEmitter } from '@angular/core';
import { FFResizeSensorService } from './ff-resize-sensor.service';
import { FFResizeSensorEvent } from './resize-event';
export declare class FFResizeSensorDirective implements AfterViewInit {
    el: ElementRef;
    private service;
    private platformId;
    width: number;
    height: number;
    oldWidth: number;
    oldHeight: number;
    readonly resize: EventEmitter<FFResizeSensorEvent>;
    constructor(el: ElementRef, service: FFResizeSensorService, platformId: any);
    ngAfterViewInit(): void;
    private onResize;
}
