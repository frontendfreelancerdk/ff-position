export declare class FFResizeSensorEvent {
    readonly el: HTMLElement;
    readonly oldWidth: number;
    readonly oldHeight: number;
    readonly width: number;
    readonly height: number;
    constructor(el: HTMLElement, oldWidth: number, oldHeight: number, width: number, height: number);
}
